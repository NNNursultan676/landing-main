# Архитектура проекта Sapa Technologies

## 🏗 Микросервисная архитектура

Проект разделен на три независимых микросервиса:

### 1. **Frontend (Основной сайт)**
- **Порт:** 5173 (по умолчанию)
- **Технологии:** React, Vite, Ant Design
- **Назначение:** Публичный сайт компании
- **Запуск:** `npm run dev`

### 2. **Admin Panel (Админ-панель)**
- **Порт:** 3001
- **Технологии:** React, Vite, Ant Design
- **Назначение:** Управление контентом сайта (вакансии, статьи, команда, продукты)
- **Запуск:** `npm run dev:admin` или `cd admin && npm run dev`
- **Доступ:** http://localhost:3001

### 3. **API Service (Backend API)**
- **Порт:** 3002
- **Технологии:** Node.js, Express
- **Назначение:** REST API для управления данными
- **Запуск:** `npm run dev:api` или `cd api && npm run dev`
- **Доступ:** http://localhost:3002

## 📁 Структура проекта

```
landing-main/
├── src/                    # Frontend (основной сайт)
│   ├── components/         # React компоненты
│   ├── layouts/            # Макеты страниц
│   ├── locales/            # Переводы (RU/KZ/EN)
│   └── services/           # Сервисы для работы с API
│
├── admin/                  # Admin Panel (микросервис)
│   ├── src/
│   │   ├── components/    # Компоненты админки
│   │   └── pages/          # Страницы админки
│   └── package.json
│
├── api/                    # API Service (микросервис)
│   ├── server.js           # Express сервер
│   ├── data/               # JSON файлы с данными
│   │   ├── vacancies.json
│   │   ├── articles.json
│   │   ├── team.json
│   │   └── products.json
│   └── package.json
│
└── package.json            # Основной package.json
```

## 🚀 Запуск проекта

### Установка всех зависимостей
```bash
npm run install:all
```

### Запуск всех сервисов одновременно
```bash
npm run dev:all
```

Это запустит:
- Frontend на http://localhost:5173
- Admin Panel на http://localhost:3001
- API на http://localhost:3002

### Запуск отдельных сервисов

**Frontend:**
```bash
npm run dev
```

**Admin Panel:**
```bash
npm run dev:admin
# или
cd admin && npm run dev
```

**API:**
```bash
npm run dev:api
# или
cd api && npm run dev
```

## 🔌 API Endpoints

### Вакансии
- `GET /api/vacancies` - Получить все вакансии
- `POST /api/vacancies` - Создать вакансию
- `PUT /api/vacancies/:id` - Обновить вакансию
- `DELETE /api/vacancies/:id` - Удалить вакансию

### Статьи/СМИ
- `GET /api/articles` - Получить все статьи
- `POST /api/articles` - Добавить статью
- `PUT /api/articles/:id` - Обновить статью
- `DELETE /api/articles/:id` - Удалить статью

### Команда
- `GET /api/team` - Получить информацию о команде
- `PUT /api/team` - Обновить информацию о команде

### Продукты
- `GET /api/products` - Получить информацию о продуктах
- `PUT /api/products` - Обновить информацию о продуктах

## 📝 Управление данными

Все данные хранятся в JSON файлах в папке `api/data/`:
- `vacancies.json` - Вакансии
- `articles.json` - Статьи/СМИ
- `team.json` - Информация о команде
- `products.json` - Информация о продуктах и клиентах

Данные можно редактировать:
1. Через админ-панель (рекомендуется)
2. Напрямую в JSON файлах

## 🔒 Безопасность

⚠️ **Важно:** В production необходимо:
- Добавить аутентификацию в админ-панель
- Настроить CORS для API
- Использовать базу данных вместо JSON файлов
- Добавить валидацию и санитизацию данных

## 📦 Деплой

### Frontend
Деплоится на GitHub Pages через GitHub Actions (см. `.github/workflows/deploy.yml`)

### Admin Panel и API
Требуют отдельного сервера (VPS, облако) для работы в production.

## 🛠 Разработка

### Добавление нового раздела в админку

1. Создать endpoint в `api/server.js`
2. Создать страницу в `admin/src/pages/`
3. Добавить роут в `admin/src/App.jsx`
4. Добавить пункт меню в `admin/src/components/Sidebar.jsx`

### Интеграция с Frontend

Frontend может получать данные через API:
```javascript
import axios from 'axios';

const API_URL = 'http://localhost:3002/api';

// Получить вакансии
const vacancies = await axios.get(`${API_URL}/vacancies`);

// Получить статьи
const articles = await axios.get(`${API_URL}/articles`);
```

## 📚 Дополнительная документация

- [README.md](./README.md) - Основная документация проекта
- [API Documentation](./api/README.md) - Документация API (если создана)
