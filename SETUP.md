# Инструкция по установке и запуску

## 🚀 Быстрый старт

### 1. Установка всех зависимостей

```bash
npm run install:all
```

Эта команда установит зависимости для:
- Основного сайта (frontend)
- API сервера
- Админ-панели

### 2. Запуск всех сервисов

```bash
npm run dev:all
```

Это запустит:
- **Frontend** на http://localhost:5173
- **Admin Panel** на http://localhost:3001
- **API** на http://localhost:3002

### 3. Доступ к сервисам

После запуска откройте в браузере:

- **Основной сайт:** http://localhost:5173
- **Админ-панель:** http://localhost:3001
- **API:** http://localhost:3002

## 📦 Установка зависимостей по отдельности

Если нужно установить зависимости для каждого сервиса отдельно:

```bash
# Основной сайт
npm install

# API сервер
cd api && npm install

# Админ-панель
cd admin && npm install
```

## 🔧 Запуск отдельных сервисов

### Frontend (основной сайт)
```bash
npm run dev
# или
npm start
```

### API сервер
```bash
npm run dev:api
# или
cd api && npm run dev
```

### Админ-панель
```bash
npm run dev:admin
# или
cd admin && npm run dev
```

## 🗄️ Инициализация данных

Данные для API автоматически создаются при первом запуске в папке `api/data/`:

- `vacancies.json` - Вакансии
- `articles.json` - Статьи/СМИ (уже заполнены начальными данными)
- `team.json` - Информация о команде
- `products.json` - Информация о продуктах

## 🔐 Работа с админ-панелью

1. Откройте http://localhost:3001
2. Используйте разделы меню для управления:
   - **Вакансии** - добавление/редактирование вакансий
   - **Статьи/СМИ** - управление статьями
   - **Команда** - редактирование информации о команде
   - **Продукты** - управление клиентами и партнерами

## ⚠️ Важные замечания

1. **API должен быть запущен** перед использованием админ-панели и для загрузки данных на основном сайте
2. **Порты должны быть свободны** - убедитесь, что порты 3001, 3002, 5173 не заняты
3. **CORS** - в production необходимо настроить CORS для API

## 🐛 Решение проблем

### Порт уже занят
Измените порт в соответствующих конфигурационных файлах:
- `vite.config.js` (frontend) - server.port
- `admin/vite.config.js` - server.port
- `api/server.js` - PORT

### API не отвечает
1. Проверьте, что API сервер запущен
2. Проверьте, что порт 3002 свободен
3. Проверьте консоль на наличие ошибок

### Админ-панель не загружает данные
1. Убедитесь, что API запущен на порту 3002
2. Проверьте консоль браузера на ошибки CORS
3. Проверьте, что URL API правильный в компонентах админки

## 📝 Дополнительная информация

- [ARCHITECTURE.md](./ARCHITECTURE.md) - Описание архитектуры проекта
- [README.md](./README.md) - Основная документация
