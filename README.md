# Sapa Technologies - Landing Page

Лендинг-страница для казахстанской IT-компании Sapa Technologies, входящей в состав группы компаний Sapa. Проект разработан на React с использованием Vite.

## 📋 Описание проекта

Одностраничный лендинг с многоязычной поддержкой (русский, казахский, английский), содержащий информацию о компании, продуктах, решениях и контактах. Проект включает форму заказа демо-версии с интеграцией почтового сервиса.

**Основные функции:**
- 🎨 Адаптивный дизайн с использованием Ant Design
- 🌍 Многоязычность (RU, KZ, EN) через i18next
- 📧 Форма отправки заявок на демо
- 🗺️ Интеграция Google Maps и 2GIS
- 📱 Мобильная навигация с Drawer
- ⚡ Быстрая загрузка благодаря Vite

## 🛠 Технологический стек

### Основные технологии
- **React** 18.3.1 - UI библиотека
- **Vite** 5.3.4 - сборщик и dev-сервер
- **Ant Design** 5.20.0 - UI компоненты
- **i18next** 23.12.2 - интернационализация

### Дополнительные библиотеки
- **Formik** + **Yup** - работа с формами и валидацией
- **react-router-dom** 6.27.0 - навигация (подключен, но не используется в одностраничном приложении)
- **axios** 1.7.7 - HTTP клиент
- **react-toastify** 10.0.6 - уведомления
- **react-input-mask** 2.0.4 - маски ввода для телефонов
- **react-multi-carousel** 2.8.5 - карусели
- **@react-google-maps/api** 2.20.3 - карты Google

### Инструменты разработки
- **ESLint** - линтинг кода
- **TypeScript** 5.5.4 - типизация (частично)
- **SASS/LESS** - препроцессоры стилей

## 📁 Структура проекта

```
landing-main/
├── .github/
│   └── workflows/
│       └── deploy.yml              # GitHub Actions для автоматического деплоя
├── public/
│   ├── documents/                  # PDF документы (политика, подтверждения)
│   │   ├── confirmation_sapa_tech.pdf
│   │   └── politics_Sapa_Technologies.pdf
│   ├── .nojekyll                   # Отключение Jekyll для GitHub Pages
│   └── vite.svg
├── src/
│   ├── assets/                     # Статические ресурсы
│   │   ├── favicon/                # Иконки сайта
│   │   └── images/                 # Изображения
│   │       ├── partners/           # Логотипы партнеров
│   │       └── ...
│   ├── components/                 # React компоненты
│   │   ├── AboutMain/              # Секция "О нас"
│   │   ├── BenefitsSection/        # Секция преимуществ
│   │   ├── ContactsMain/           # Секция контактов
│   │   ├── CTASection/             # Call-to-action секция
│   │   ├── CustomButton/           # Переиспользуемая кнопка
│   │   ├── CustomText/             # Переиспользуемый текст
│   │   ├── DemoModal/              # Модальное окно заявки на демо
│   │   ├── Main/                   # Главная секция (hero)
│   │   ├── PartnersMain/           # Секция партнеров
│   │   ├── PartnersRunner/         # Анимация логотипов партнеров
│   │   ├── PartnersSection/        # Карточки партнеров
│   │   ├── ProductsMain/           # Секция продуктов
│   │   ├── ProductModal/           # Модальное окно продукта
│   │   ├── ServiceSection/         # Секция услуг
│   │   ├── Slider/                 # Компонент слайдера
│   │   ├── SolutionCard/           # Карточка решения
│   │   ├── SolutionsSection/       # Секция решений
│   │   ├── StatCard/               # Карточка статистики
│   │   ├── StatsSection/           # Секция статистики
│   │   ├── TagLayout/              # Теги/чипы
│   │   └── UnifiedFeatures/        # Объединенные функции
│   ├── layouts/                    # Макеты страниц
│   │   ├── About/                  # Страница "О нас"
│   │   ├── Contacts/               # Страница контактов
│   │   ├── Footer/                 # Футер сайта
│   │   ├── MainLayout/             # Главный макет (используется)
│   │   ├── Nav/                    # Навигационное меню
│   │   ├── Partners/               # Страница партнеров
│   │   └── Products/               # Страница продуктов
│   ├── locales/                    # Файлы переводов
│   │   ├── ru/                     # Русский язык
│   │   │   └── translation.json
│   │   ├── kz/                     # Казахский язык
│   │   │   └── translation.json
│   │   └── en/                     # Английский язык
│   │       └── translation.json
│   ├── services/                   # Сервисы
│   │   ├── mailService.js          # Сервис отправки почты
│   │   ├── mail.js                 # Дополнительные функции почты
│   │   ├── modalService.js         # Сервис управления модальными окнами
│   │   └── tokenService.js         # Сервис работы с токенами
│   ├── shared/
│   │   └── config/
│   │       └── i18n.js             # Конфигурация интернационализации
│   ├── lib/
│   │   └── classNames/
│   │       └── classNames.ts       # Утилита для классов CSS
│   ├── App.jsx                     # Главный компонент приложения
│   ├── App.css                     # Глобальные стили приложения
│   ├── AntReset.css                # Сброс стилей для Ant Design
│   ├── index.css                   # Базовые стили
│   └── main.jsx                    # Точка входа приложения
├── .eslintrc.cjs                   # Конфигурация ESLint
├── .gitignore                      # Игнорируемые файлы Git
├── .gitlab-ci.yml                  # GitLab CI/CD конфигурация
├── app.js                          # Express сервер для проксирования API (dev)
├── index.html                      # HTML шаблон
├── nginx.conf                      # Конфигурация Nginx для продакшена
├── package.json                    # Зависимости и скрипты
├── vite.config.js                  # Конфигурация Vite
└── README.md                       # Документация проекта
```

## 🚀 Требования

- **Node.js** версия 16 или выше
- **npm** или **yarn**

## 📦 Установка и запуск проекта

### 1. Клонировать репозиторий

```bash
git clone <URL_РЕПОЗИТОРИЯ>
cd landing-main
```

### 2. Переключиться на нужную ветку

```bash
git checkout <НАЗВАНИЕ_ВЕТКИ>
```

### 3. Установить зависимости

```bash
npm install
```

### 4. Запустить dev-сервер

```bash
npm run dev
```

После запуска проект будет доступен по адресу: **http://localhost:5173/**

**Примечание:** В режиме разработки Vite настроен прокси для API запросов:
- `/api/*` → `https://dev.callback.sdf.cmp.pkz.icdc.io/*`
- `/token/*` → `https://dev.callback.sdf.cmp.pkz.icdc.io/*`

## 📜 Доступные команды

- `npm run dev` - запустить dev-сервер
- `npm run build` - собрать проект для продакшена
- `npm run preview` - предпросмотр собранного проекта
- `npm run lint` - проверить код линтером ESLint
- `npm run deploy` - собрать и задеплоить на GitHub Pages (требует настройки)

## 🌐 Интернационализация

Проект поддерживает три языка:
- **Русский (ru)** - язык по умолчанию
- **Казахский (kk/kz)**
- **Английский (en)**

Язык определяется автоматически из:
1. LocalStorage (ключ `locale`)
2. Браузера пользователя

Переключение языка осуществляется через выпадающее меню в навигации.

## 🔧 Конфигурация

### Vite (vite.config.js)

- **Base path:** настраивается через переменную окружения `VITE_BASE_PATH`
- **Aliases:** настроены пути `@`, `components`, `images`, `utils` и др.
- **Proxy:** настроен для `/api` и `/token` endpoints

### API Proxy

В режиме разработки запросы проксируются через Vite dev server на:
- `https://dev.callback.sdf.cmp.pkz.icdc.io`

### Production (nginx.conf)

В продакшене Nginx настроен для:
- Проксирования `/api/` запросов
- Обслуживания статических файлов
- SPA routing (fallback на `index.html`)

## 🚀 Деплой

### GitHub Pages (автоматический)

1. **Включите GitHub Pages в настройках репозитория:**
   - Settings → Pages
   - Source: "GitHub Actions"

2. **Настройте workflow файл:**
   - Откройте `.github/workflows/deploy.yml`
   - Измените `branches: - main` на вашу ветку
   - Измените `VITE_BASE_PATH: /landing-main/` на название вашего репозитория
     - Для `username.github.io` → `VITE_BASE_PATH: /`
     - Для других репозиториев → `VITE_BASE_PATH: /repository-name/`

3. **Запушьте изменения:**
   ```bash
   git add .
   git commit -m "Настройка деплоя"
   git push
   ```

4. Деплой запустится автоматически при пуше в указанную ветку.

### GitHub Pages (ручной)

```bash
# Настройте base path в vite.config.js
# Затем выполните:
npm run deploy
```

### GitLab CI/CD

Проект включает конфигурацию `.gitlab-ci.yml` для автоматического деплоя через GitLab CI/CD.

### Production сервер

Для деплоя на production сервер:
1. Соберите проект: `npm run build`
2. Настройте Nginx согласно `nginx.conf`
3. Разместите содержимое папки `dist/` на сервере

## ⚠️ Важные замечания

### Ограничения GitHub Pages

**Что НЕ будет работать:**
- ❌ API запросы к внешним серверам (прокси не работает)
- ❌ Отправка форм через почтовый сервис (требуется backend)

**Что будет работать:**
- ✅ Весь интерфейс и навигация
- ✅ Статический контент
- ✅ Якорные ссылки (#about, #solutions, #contacts)
- ✅ Переключение языков
- ✅ Анимации и интерактивные элементы

### API Endpoints

Формы на сайте отправляют данные на:
- `/send` (проксируется через `/api/send`)

В продакшене эти запросы должны обрабатываться backend сервером или почтовым сервисом.

## 🎨 Компоненты и функции

### Основные секции

- **Hero Section** - главный баннер с призывом к действию
- **About Section** - информация о компании, статистика (25% оптимизация, 250+ партнеров, 95% лояльность)
- **Solutions Section** - три основных решения:
  - Кредитный конвейер
  - Кредитный брокер
  - Индивидуальные решения
- **Partners Section** - логотипы партнеров (20+ компаний)
- **Contacts Section** - контактная информация, форма обратной связи, карта Google Maps, ссылка на 2GIS

### Модальные окна

- **DemoModal** - форма заказа демо-версии с полями:
  - Имя
  - Название компании
  - Телефон (с маской +7 (7**) ***-**-**)

### Навигация

- Фиксированная навигация с плавной прокруткой к секциям
- Мобильное меню с Drawer
- Автоматическое определение активной секции при прокрутке
- Переключение языков через dropdown

## 📞 Контакты

Согласно информации на сайте:
- **Телефон:** +7 775 880 52 34
- **Email:** info@sapatech.kz
- **Адрес:** г. Алматы, ул. Зеина Шашкина, д. 24

## 📄 Документы

В папке `public/documents/` находятся:
- `confirmation_sapa_tech.pdf` - подтверждение
- `politics_Sapa_Technologies.pdf` - политика компании

## 🔍 Дополнительная информация

### Используемые сервисы

- Google Maps API - для отображения карты
- 2GIS - интеграция карт
- Почтовый сервер - для отправки заявок (настраивается через backend)

### Браузерная поддержка

Проект использует современные стандарты и поддерживает:
- Chrome (последние версии)
- Firefox (последние версии)
- Safari (последние версии)
- Edge (последние версии)

## 📝 Лицензия

Проект является коммерческим продуктом компании Sapa Technologies.

## 👥 Разработка

Для работы над проектом:
1. Создайте feature-ветку от `main`
2. Внесите изменения
3. Проверьте линтером: `npm run lint`
4. Протестируйте локально: `npm run dev`
5. Создайте Pull Request

---

**Sapa Technologies** - Эксперт в области разработки цифровых и кастомных решений любой сложности.
